# Website Notes

This is a lightweight Jekyll site for baillieai.com.

## Local development

```powershell
bundle install
bundle exec jekyll serve --host 127.0.0.1 --port 8080
```

## Hiking data

Munro cards are listed in _data/climbed_munros.yml. Report pages live in _hikes/.
